export type ShowCategoryOverrides = Record<string, string[]>;

export const SHOW_CATEGORY_OVERRIDES: ShowCategoryOverrides = {
  "what if everyone were reborn with their memories": ["naruto"],
  "what if the village got their wish": ["naruto"],
  "what if anime worlds collided": ["naruto", "one-piece", "dragon-ball"],
};
